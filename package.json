{
  "name": "meanapp",
  "version": "1.0.0",
  "description": "Trial app to learn mean stack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "nodemon --exec babel-node ./index.js"
  },
  "keywords": [
    "mean"
  ],
  "author": "Christine",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "express-promise-router": "^3.0.3",
    "mongoose": "^5.3.13"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^1.18.7",
    "rimraf": "^2.6.2"
  }
}
